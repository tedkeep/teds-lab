<%= render MainContainer::Component.new do %>
  <div class="mb-5 border-b border-gray-200">
    <h1>Posts 📖</h1>
  </div>

  <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
    <% @posts.each do |post| %>
      <%= render Card::Component.new(title: post.title, description: l(post.published_on), link: post.permalink) %>
    <% end %>
  </div>
<% end %>
