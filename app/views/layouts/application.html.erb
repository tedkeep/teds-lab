<!DOCTYPE html>
<html>
  <head>
    <title>Teds Lab</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
    <nav class="flex flex-wrap items-center shadow-md justify-start py-2 px-5 lg:px-40">
      <!-- logo -->
      <img src="http://acmelogos.com/images/logo-1.svg" alt="ACME" width="120" />
      <!-- hamburger -->
      <div class="flex ml-auto md:hidden">
        <button id="hamburger">
          <img class="toggle block" src="https://img.icons8.com/fluent-systems-regular/2x/menu-squared-2.png" width="40" height="40" />
          <img class="toggle hidden" src="https://img.icons8.com/fluent-systems-regular/2x/close-window.png" width="40" height="40" />
        </button>
      </div>
      <!-- links -->
      <div class="toggle hidden md:ml-5 md:flex w-full md:w-auto text-right text-bold mt-5 md:mt-0 border-t-2 border-purple-700 md:border-none">
        <% [
             {link: root_path, text: 'Home'},
             {link: blog_index_path, text: 'Blog'},
           ].each do |nav_link| %>
          <%= render NavbarLink::Component.new(link: nav_link[:link], text: nav_link[:text]) %>
        <% end %>
      </div>
      <!-- social links -->
      <a href="#" class="toggle hidden md:ml-auto md:flex w-full md:w-auto px-4 py-2 text-right bg-purple-900 hover:bg-purple-700 text-white md:rounded">Create Account</a>
    </nav>
  </header>

  <script>
      document.getElementById("hamburger").onclick = function toggleMenu() {
          const navToggle = document.getElementsByClassName("toggle");
          for (let i = 0; i < navToggle.length; i++) {
              navToggle.item(i).classList.toggle("hidden");
          }
      };
  </script>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

  <%= yield %>

  </body>
</html>
